name: "Hello World Workflow"

on:
  push:
    branches:
      - master
      - production

jobs:
  hello_world:
    runs-on: ubantu-latest
    steps:
      - name: "print hello world"
        run: "echo hello world"
